/*
 * domain: pure Java. No Spring, no JGit, no SVNKit, no other project modules.
 * Only JDK + test libraries are allowed here.
 */
plugins {
    `java-library`
}

dependencies {
    // Intentionally empty: the domain layer must not depend on anything.
}
